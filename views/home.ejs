<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <style>
        #wrapper{
            background: black;
            height:100vh;
            padding:1rem 0px;
        }
        .container{
            border:1px solid wheat;
            border-radius: 5px;
            padding:10px;
        }
        .sender{
            float: right;
            clear: both;
        }
        .receiver{
            float: left;
            clear: both;
        }
        .message{
            padding: 10px;
            margin: 10px 15px;
            border: 1px solid darkgray;
            border-radius: 5px;
        }
        #message-container{
            height: 80vh;
            background: black;
            color: aqua;
            overflow-y:scroll;
        }
        form{
            display: flex;
            position: relative;
        }
        input{
            flex: 1;
            padding: 10px;
            border: 2px solid white;
            border-radius: 10px;
        }
        #btn{
            position: absolute;
            right: 20px;
            top: 10px;
            background: transparent;
            border:none;
            margin-right: 10px;
            width:20px;
            height:20px;
        }
        .joined{
            color: white;
        /* width: 241px; */
            width: max-content;
             max-width: 250px;
            text-align: center;
            margin-left:auto;
            margin-right: auto;
        /* text-align: center; */
        }
    </style>
</head>
<body>
    <div id="wrapper">
        <div class="container">
            <div id="message-container">
            </div>
            <div id="message-sender">
                <form action="" id="form">
                    <input type="text" placeholder="Enter your message" id="data">
                    <button  id="btn" type="submit">
                        <img src="/images/right-arrow.png" alt="send">
                    </button>
                </form>
            </div>
        </div>
    </div>
    <script src="http://localhost:3000/socket.io/socket.io.js"></script>
    <script src="/client.js"></script>
</body>
</html>